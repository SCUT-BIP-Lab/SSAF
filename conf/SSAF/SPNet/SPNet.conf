### 总控配置 ###
task_name = SPNet_all # 实验名称(每一个实验建议新建一个conf文件，然后设置不同的task_name)
home_self = /home/zyf/code/dhg/ # 代码目录位置
model_save_dir = ${home_self}/workdir/checkpoint/${task_name} # 模型存储地址
log_file_dir = ${home_self}/workdir/log/${task_name} # log文件地址
tb_file_dir = ${home_self}/workdir/tb/${task_name} # tensorboard地址
ckpt_name = best.pth
data_save_dir = /home/zyf/results/spnet
init_from = /home/zyf/code/dhg//workdir/checkpoint/SPNet_all/best.pth

### 数据 ###
modality = RGB, mask, kpt_heatmap
dataset_root =/home/data/DHG-Auth
frames_root = ${dataset_root}/color_hand, ${dataset_root}/mask, ${dataset_root}/keypoints_wxl

dataset_root2 =/home/data/DHG-Auth
frames_root2 =${dataset_root2}/color_background, ${dataset_root2}/mask, ${dataset_root2}/keypoints_wxl

#dataset_root3 = /home/data/Real-DHGA
#frames_root3 = ${dataset_root3}/color_raw

eval_modality = RGB
eval_dataset_root = /home/data/Real-DHGA
eval_frames_root = ${eval_dataset_root}/color
frames_per_video = all # 每个视频抽帧数量
input_frame_size = 224


### 模型配置 ###
model_path = SSAF.SPNet
model_name = Model_SPNet


### 训练控制参数 ###
num_epochs=100  # 迭代轮次上限
batchsize_train = 100
batchsize_test = 200
train_dataloader_workers = 4 #训练dataloader workers
eval_dataloader_workers = 4 # 验证dataloader workers
optimizer=adam  # 优化器
learning_rate=0.001  # 学习率[pretrained_parms,new_params]
new_lr_step=10  # 每n个epoch学习率下降
lr_gamma=0.45  # 学习率策略
save_model = True  # 是否储存模型
eval_internal = 34

# extra
description = "MG setup, tsn for video, agcn for skeleton, 4 layers, nonlocal for feature interaction at layer4, video feature is detached"